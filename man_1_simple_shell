.TH man 1 "14 November 2017" "0.01" "Olaf The Shell man page"
.SH NAME
.B Olaf
- A basic UNIX command line interpreter.
.SH SYNOPSIS
 Olaf can interpret and execute command line arguments read from the standard input.
.SH DESCRIPTION
 The Olaf shell is a command that read line by line from a file or the terminal line.

 It then interprets the lines, and if at the begining of the line there is a valid

 command it then executes it. The Olaf shell will parse to the environemtal variables

 to find the path for commands.

.SH PATH Search
 When looking for a command, Olaf will first look to see if  there are any buildin functions

 with that name and then execute them. If not buitins found then the PATH will be parsed to try to find the command executable. If the full

 path of a function is entered it will just simply run it.
.SH USAGE
All the files are to be compiled on an Ubuntu 14.04 LTS machine with `gcc -Wall -Werror -Wextra -pedantic *.c -o Olaf`

Once compiled, to login run:
 ($) ./Olaf
.SH RETURN VALUES
Olaf shell will exit with status 1.
.SH SEE ALSO
.I sh(1)
.SH BUGS
No known bugs.
.SH AUTHOR
Steven Garcia